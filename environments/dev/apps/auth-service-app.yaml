apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-auth-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/WashifyApp/washify-gitops.git'
    targetRevision: main
    path: charts/base-service
    helm:
      values: |
        image:
          repository: ghcr.io/washifyapp/auth-service
          tag: latest
        containerPort: 8080
      valueFiles:
        - ../../global-values/dev-values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dev-env
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
